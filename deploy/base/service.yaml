apiVersion: v1
kind: Service
metadata:
  name: backstage
  namespace: backstage
  labels:
    app.kubernetes.io/name: backstage
    app.kubernetes.io/part-of: backstage
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: backstage
  ports:
    - port: 7007
      targetPort: http
      nodePort: 30007
      name: http
