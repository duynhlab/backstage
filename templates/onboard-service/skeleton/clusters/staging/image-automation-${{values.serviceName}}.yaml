# Flux image automation for ${{ values.serviceName }} on staging.
# Rewrites the marked image.tag in apps/staging/${{ values.serviceName }}/values-staging.yaml
# and commits to main (staging is the auto-deploy lane). Tracks the latest
# semver release image (X.Y.Z, built by CI on v* tag pushes).
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: ${{ values.serviceName }}-staging
  namespace: flux-system
spec:
  image: ${{ values.imageRepository }}
  interval: 5m
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ${{ values.serviceName }}-staging
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: ${{ values.serviceName }}-staging
  filterTags:
    pattern: '^(?P<version>[0-9]+\.[0-9]+\.[0-9]+)$'
    extract: '$version'
  policy:
    semver:
      range: '>=0.1.0'
